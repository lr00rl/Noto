<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened runtime entitlements for notarization.

    Electron needs the JIT and unsigned-memory exceptions for V8. Everything
    else stays off: Noto has no camera, microphone, location or network
    entitlement, because the app does not use them and the plugin runtime is
    explicitly denied network access.

    user-selected.read-write is what lets the open and save dialogs hand the app
    a file. It is scoped to what the user actually picks.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
